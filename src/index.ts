function hello(nama: string, umur: number): string {
    return `Selamat Datang ${nama} di Pontianak, umur kamu ${umur} tahun`;
}

const mahasiswa = "Aurell";
const umur: number = 19;

console.log(hello(mahasiswa, umur));